rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /calc3d_users/{userId}/app/{docId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
